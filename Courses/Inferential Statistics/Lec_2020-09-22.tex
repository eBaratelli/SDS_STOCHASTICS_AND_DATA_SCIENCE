% Lec_22-09-2020

\prop{Chebyshev Inequality}
Given a random variable $X$ with finite second moment, with $\sigma = \sqrt{Var(X)}$, for any $h >0$ we have:

	$\PP(\abs{X-\EE(X)} \geq h \sigma ) <\leq \ \dfrac{1}{h^2} $
